import './Footer.css';

const Footer = () => {
    return (
        <footer className="footer">
            <div className="under">
            <a href="#">Contact</a>
              <a href="#">About</a>
              <a href="#">Home</a>
            </div>
            <div className="over">
            <a href="#">Sign In / Register</a>
            </div>
        </footer>
    )
}

export default Footer;